<div class="menu menu--contact">
  <ul class="menu__list">
    <% data.menus.contact.each do | item | %>
      <li class="menu__item">
        <a class="menu__link" href="<%= item.link %>" title="<%= item.title %>" target="_blank">
          <span class="menu__copy"><%= item.title %></span>
        </a>
      </li>
    <% end %>
  </ul>
</div>
